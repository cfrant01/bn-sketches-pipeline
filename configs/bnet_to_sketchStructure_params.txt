# Parameters for bnet_to_sketchStructure.py (key = value)

# Required
bnet = outputs/bnet/net.bnet
output = outputs/sketch_parts/model_part_from_config.aeon

# Reveal controls (can be used together)
reveal_functions_percent = 50
reveal_regulators_percent = 50

# Optional
seed = 42
edge_op = -??
hidden_policy = omit
